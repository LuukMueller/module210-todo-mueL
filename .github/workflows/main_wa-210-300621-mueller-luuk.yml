name: Build and Deploy Docker App to Azure Web App

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to Docker registry
      uses: docker/login-action@v2
      with:
        registry: 'acr300621210.azurecr.io'
        username: ${{ secrets.AzureAppService_ContainerUsername_661c6631022447c7aab795e4d73f6fa3 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_96aa261e36e5494e837263ff626bbed4 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: acr300621210.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_661c6631022447c7aab795e4d73f6fa3 }}/todo-app:${{ github.sha }}
        context: ./src/backend
        file: ./src/backend/Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'wa-210-300621-mueller-luuk'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_04aa154bcf764ce1ae3a036ed68f4f7b }}
        images: 'acr300621210.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_661c6631022447c7aab795e4d73f6fa3 }}/todo-app:${{ github.sha }}'
